---
title: 'Country endpoints'
description: 'An explication for a countries endpoints'
---

import CodeExample from '../../../../components/api-endpoints/code-example.astro';

import CodeResponseCountryAll from '../../../../components/api-endpoints/country/all/all-response.country.astro';
import CodeResponseCountryDetails from '../../../../components/api-endpoints/country/details/details-response.country.astro';

import GlobalHeaderNote from '../../../../components/aside/note/global-header-params.astro';
import GlobalQueryNote from '../../../../components/aside/note/global-query-params.astro';
import DetailsQueryNote from '../../../../components/aside/note/details-query-params.astro';

import QueryParamsExplanation from '../../../../components/repetitive-texts/query-params-explanation.astro';

In this section we will show you the **`country`** endpoint, its parameters, combinations and specific details.\
At the moment, <a href="/" id="link" class="no-underline">Countries Sundries</a> covers a total of 160 countries and different international organizations.

## Endpoint response and parameters

This endpoint will return the information of the countries with limit of 30 registers.

## `country/all`
The `country/all` endpoint, if no limit is specified, will by default return 30 countries from the database.

### Example Request

<CodeExample endpoint="country/all" />

:::note[Keep in mind...]
  The fewer countries registers you bring in, the faster the server will respond.
:::

### Response
>The result of the request will be a JSON object with the information of the countries.

<CodeResponseCountryAll />

### Header Parameters
<GlobalHeaderNote />

### Query Parameters
<QueryParamsExplanation />

#### `id`
> _Optional Integer_\
Allows you to filter registers by id
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?id=1'
```

#### `name`
> _Optional String_\
Allows you to filter registers by name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?name=Argentina'
```

#### `official_name`
> _Optional String_\
Allows you to filter registers by official name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?official_name=Republic%20of%20Argentina'
```

#### `abbr`
> _Optional String_\
Allows you to filter registers by abbreviation
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?abbr=ARG'
```

#### `internet_tld`
> _Optional String_\
Allows you to filter registers by internet tld
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?internet_tld=.ar'
```

#### `motto`
> _Optional String_\
Allows you to filter registers by motto
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?motto=En%20unión%20y%20libertad'
```

#### `anthem`
> _Optional String_\
Allows you to filter registers by anthem
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?anthem=Himno%20Nacional%20Argentino'
```

#### `capital`
> _Optional String_\
Allows you to filter registers by capital
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?capital=Buenos%20Aires'
```

<GlobalQueryNote />

#### `official_language`
> _Optional String_\
Allows you to filter registers by official language
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?official_language=Spanish'
```

#### `iso_3166_1_alpha_2`
> _Optional String_\
Allows you to filter registers by ISO 3166-1 alpha-2
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?iso_3166_1_alpha_2=AR'
```

#### `iso_3166_1_alpha_3`
> _Optional String_\
Allows you to filter registers by ISO 3166-1 alpha-3
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?iso_3166_1_alpha_3=ARG'
```

#### `calling_code`
> _Optional String_\
Allows you to filter registers by calling code
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?calling_code=+54'
```

#### `timezone`
> _Optional String_\
Allows you to filter registers by timezone
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?timezone=UTC-3'
```

#### `id_coat_of_arms_fk`
> _Optional Integer_\
Allows you to filter registers by id coat of arms foreign key
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?id_coat_of_arms_fk=1'
```

#### `coat_of_arms_name`
> _Optional String_\
Allows you to filter registers by coat of arms name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?coat_of_arms_name=Escudo%20de%20la%20Rep%C3%BAblica%20Argentina'
```

#### `coat_of_arms_type`
> _Optional String_\
Allows you to filter registers by coat of arms type
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?coat_of_arms_type=svg'
```

#### `id_flag_fk`
> _Optional Integer_\
Allows you to filter registers by id flag foreign key
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?id_flag_fk=1'
```

#### `flag_name`
> _Optional String_\
Allows you to filter registers by flag name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?flag_name=Argentina'
```

#### `flag_type`
> _Optional String_\
Allows you to filter registers by flag type image
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?flag_type=svg'
```

#### `id_currency_fk`
> _Optional Integer_\
Allows you to filter registers by id currency foreign key
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?id_currency_fk=1'
```

#### `currency_name`
> _Optional String_\
Allows you to filter registers by currency name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?currency_name=Argentine%20Peso'
```

#### `currency_abbr`
> _Optional String_\
Allows you to filter registers by currency abbreviation
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?currency_abbr=ARS'
```

#### `currency_name`
> _Optional String_\
Allows you to filter registers by currency name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?currency_name=Argentine%20Peso'
```

#### `id_region_fk`
> _Optional Integer_\
Allows you to filter registers by id region foreign key
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?id_region_fk=1'
```

#### `region_name`
> _Optional String_\
Allows you to filter registers by region name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?region_name=America'
```

#### `id_sub_region_fk`
> _Optional Integer_\
Allows you to filter registers by id sub region foreign key
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?id_sub_region_fk=1'
```

#### `sub_region_name`
> _Optional String_\
Allows you to filter registers by sub region name
```bash
curl -X GET 'http://countries-sundries-api.com/country/all?sub_region_name=South%20America'
```

## `country/details`
This endpoint will allow you to obtain all the previous information but returning a single register, this will allow a much faster response if you need a single country and not many.\
All Query Parameters (except <a href="/docs/api-reference/global-query-parameters/" id="link" class="no-underline">Global Query Parameters</a>) are enabled for `details`.\
If no Query Parameter is specified, the first register will be returned.

### Example Request
>We will make an example of a possible API request that could be given in case you want to bring the details of a specific country.

<CodeExample endpoint="country/details" />

### Response
>The result of the request will be a JSON object with the information of the country.

<CodeResponseCountryDetails />

### Header Parameters
<GlobalHeaderNote />

### Query Parameters
<DetailsQueryNote />