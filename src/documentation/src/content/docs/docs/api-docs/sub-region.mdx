---
title: 'Sub Region endpoint'
---

import CodeExampleSubRegionAll from '../../../../components/api-endpoints/sub-region/all-code-example.sub-region.astro';
import CodeResponseSubRegionAll from '../../../../components/api-endpoints/sub-region/all-response.sub-region.astro';

import GlobalHeaderNote from '../../../../components/aside/note/global-header-params.astro';
import GlobalQueryNote from '../../../../components/aside/note/global-query-params.astro';

In this section we will show you the **`sub-region`** endpoint, its parameters, combinations and specific details.

## Endpoint response and parameters

The `sub-region/all` endpoint will return the information of the countries of the subregions with no limit of registers brought from the database.

## `sub-region/all`
The `region/all` endpoint, if no limit is specified, will default to returning all subregions in the database.

### Example Request

<CodeExampleSubRegionAll />

### Response
> The response will be a JSON object with the information of the countries of the subregions.

<CodeResponseSubRegionAll />

### Header Parameters
<GlobalHeaderNote />

### Query Parameters
In the Query Parameters sections we will use the [CURL](https://curl.se/) command to show how to use parameters in the request. These query parameter tests will also be possible in [Postman](https://www.postman.com/), [Insomnia](https://insomnia.rest/) or other API testing tools, for example.\

#### `id`
> _Optional Integer_\
Allows you to filter records by id
```bash
curl -X GET 'http://countries-sundries-api.com/region/all?id=1'
```

#### `name`
> _Optional String_\
Allows you to filter records by name
```bash
curl -X GET 'http://countries-sundries-api.com/region/all?name=America'
```

<GlobalQueryNote />

#### `id_region_fk`
> _Optional Integer_\
Allows you to filter records by id region foreign key from the region information in the database.
```bash
curl -X GET 'http://countries-sundries-api.com/region/all?id_region_fk=1'
```