---
title: 'Currency endpoints'
---

import CodeExample from '../../../../components/api-endpoints/code-example.astro';

import CodeResponseCurrencyAll from '../../../../components/api-endpoints/currency/all/all-response.currency.astro';
import CodeResponseCurrencyDetails from '../../../../components/api-endpoints/currency/details/details-response.currency.astro';

import GlobalHeaderNote from '../../../../components/aside/note/global-header-params.astro';
import GlobalQueryNote from '../../../../components/aside/note/global-query-params.astro';
import DetailsQueryNote from '../../../../components/aside/note/details-query-params.astro';

import QueryParamsExplanation from '../../../../components/repetitive-texts/query-params-explanation.astro';

In this section we will show you the **`Currency`** endpoints, its parameters, combinations and specific details.\
At the moment, <a href="/" class="no-underline">Countries Sundries</a> covers a total of 161 currencies, these include those of all available countries.

## Endpoint response and parameters

This endpoint will return the information of the currencies with limit of 30 registers.

## `currency/all`
The `currency/all` endpoint, if no limit is specified, will by default return 30 currencies from the database.

### Example Request

<CodeExample endpoint="currency/all" />

:::note[Keep in mind...]
  The fewer currencies registers you bring in, the faster the server will respond.
:::

### Response
>The result of the request will be a JSON object with the information of the countries.

<CodeResponseCurrencyAll />

### Header Parameters
<GlobalHeaderNote />

### Query Parameters
<QueryParamsExplanation />

#### `id`
>_Optional Integer_\
Allows you to filter registers by id
```bash
curl -X GET "http://countries-sundries-api.com/currency/all?id=1" -H "x-countries_sundries_api-key: YOUR_API_KEY" -H "x-api-version: 1" -H "Content-Type: application/json"
```

#### `name`
>_Optional String_\
Allows you to filter registers by name
```bash
curl -X GET "http://countries-sundries-api.com/currency/all?name=Argentine%20Peso" -H "x-countries_sundries_api-key: YOUR_API_KEY" -H "x-api-version: 1" -H "Content-Type: application/json"
```

<GlobalQueryNote />

#### `abbr`
>_Optional String_\
Allows you to filter registers by abbreviation
```bash
curl -X GET "http://countries-sundries-api.com/currency/all?abbr=ARS" -H "x-countries_sundries_api-key: YOUR_API_KEY" -H "x-api-version: 1" -H "Content-Type: application/json"
```

## `currency/details`
This endpoint will allow you to obtain all the previous information but returning a single record, giving a much faster response if you only need one currency.\
All query parameters (except <a href="/docs/api-reference/global-query-parameters/" class="no-underline">Global Query Parameters</a>) are enabled for "details".\
If no query parameter is specified, the first record will be returned.

### Example Request
>We will make an example of a possible API request that could be given in case you want to bring the details of a specific currency.

<CodeExample endpoint="currency/details" />

### Response
>The result of the request will be a JSON object with the information of the currency.

<CodeResponseCurrencyDetails />

### Header Parameters
<GlobalHeaderNote />

### Query Parameters
<DetailsQueryNote />